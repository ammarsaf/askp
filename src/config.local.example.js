// src/config.local.example.js
// Copy this file to config.local.js and modify the values for your local environment
const localConfig = {
    API_URL: 'http://localhost:8787/', // Change this to your local worker URL
};

export default localConfig; 